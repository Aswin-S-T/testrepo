import { Request, Response } from 'express';

/**
 * Check parameters and generate alerts
 * @method generateAlerts
 * @param
 */
export const generateAlerts = (deviceDetails: any, sensorData: any) => {

}